# Production Environment Variables
# SECURITY: Never commit this file with real values to version control!

# PostgreSQL Configuration
POSTGRES_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# OpenAI API Key (REQUIRED)
OPENAI_API_KEY=
CLAUDE_API_KEY=

# JWT Secret (CRITICAL: Generate a secure random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_STRING

# Optional: Specify backend URL for frontend build
# Only needed if deploying frontend separately
VITE_BACKEND_URL=http://backend:5002

# --- AWS Configuration (for mTurk Integration) ---
# Optional: Fill these in if you plan to use the mTurk integration.
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
MTURK_REGION_NAME=us-east-1
MTURK_ENVIRONMENT=production
